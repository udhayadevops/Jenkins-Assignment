events {
    worker_connections 1024; # Set this to an appropriate value based on your server's requirements
}

http {
    # Your HTTP server configuration goes here
    server {
        listen 80;
        server_name localhost;

        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
        }
    }
}
